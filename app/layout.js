export const metadata = {
    title: 'TableRepair AI',
    description: 'Repare tabelas quebradas com IA',
}

export default function RootLayout({ children }) {
    return (
          <html lang="pt-BR">
            <body style={{ margin: 0, fontFamily: 'system-ui, sans-serif' }}>{children}</body>
  </html>
  )
}
